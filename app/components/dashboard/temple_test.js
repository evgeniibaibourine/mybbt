'use strict';

describe('myApp.dashboard module', function() {
    beforeEach(module('myApp.dashboard'));

    describe('temple controller', function() {

        it('should ....', inject(function($controller) {
            //spec body
            var templeCtrl = $controller('TempleCtrl');
            expect(templeCtrl).toBeDefined();
        }));

    });
});